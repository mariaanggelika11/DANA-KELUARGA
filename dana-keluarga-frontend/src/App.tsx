import { useEffect, useState, type FormEvent } from 'react'
import { Bell, ChevronDown, CircleHelp, LayoutDashboard, Menu, Users, HandCoins, CalendarClock, ArrowUpRight, ArrowDownRight, Settings, X, UserPlus, ShieldCheck, WalletCards } from 'lucide-react'
import './App.css'
import { api } from './lib/api-client'

const number = new Intl.NumberFormat('id-ID', { maximumFractionDigits: 0 })
const rupiah = (value: string | number) => `Rp ${number.format(Number(value) || 0)}`
const rupiahInput = (value: string) => value ? number.format(Number(value)) : ''
const date = new Intl.DateTimeFormat('id-ID', { day: '2-digit', month: 'short', year: 'numeric' })
const loanStatus: Record<string, string> = { PENDING: 'Menunggu persetujuan', APPROVED: 'Disetujui, menunggu pencairan', ACTIVE: 'Berjalan', REJECTED: 'Ditolak', PAID_OFF: 'Lunas', CANCELLED: 'Dibatalkan' }
const installmentStatus: Record<string, string> = { UNPAID: 'Belum dibayar', PARTIAL: 'Sebagian dibayar', PAID: 'Lunas', OVERDUE: 'Terlambat' }
const temporaryPassword = () => { const alphabet = 'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789'; const values = new Uint32Array(12); crypto.getRandomValues(values); return Array.from(values, (value) => alphabet[value % alphabet.length]).join('') }
type Summary = { balance: number; loans: number; installments: number; members: number }
type Activity = { id: string; title: string; detail: string; amount: string | number; direction: 'IN' | 'OUT'; createdBy?: { name: string } }
type Installment = { id: string; installmentNumber: number; dueDate: string; principalAmount: string | number; paidAmount: string | number; remainingAmount: string | number; status: string }
type Loan = { id: string; principalAmount: string | number; tenorMonths: number; purpose: string; status: string; requestedAt: string; rejectionReason?: string | null; approvedAt?: string | null; approvedBy?: { id: string; name: string } | null; rejectedBy?: { id: string; name: string } | null; borrower: { id: string; name: string; phone: string }; installments: Installment[] }
type User = { id: string; name: string; email: string | null; phone: string; systemRole: string; familyRole?: string; familyId?: string; familyName?: string }
type Member = { id: string; role: string; status: string; joinedAt: string; user: { name: string; email: string | null; phone: string; systemRole: string } }
type Family = { id: string; name: string; code: string }
type LedgerEntry = Activity & { occurredAt: string; description: string }
type Notice = { message: string; tone: 'success' | 'warning' | 'error' }

function App() {
  const [user, setUser] = useState<User | null>(null)
  const [loginForm, setLoginForm] = useState({ email: '', password: '' })
  const [loginError, setLoginError] = useState('')
  const [loginLoading, setLoginLoading] = useState(false)
  const [active, setActive] = useState('Ringkasan')
  const [menuOpen, setMenuOpen] = useState(false)
  const [accountMenuOpen, setAccountMenuOpen] = useState(false)
  const [summary, setSummary] = useState<Summary | null>(null)
  const [ledger, setLedger] = useState<LedgerEntry[]>([])
  const [ledgerLoading, setLedgerLoading] = useState(false)
  const [ledgerFormOpen, setLedgerFormOpen] = useState(false)
  const [ledgerForm, setLedgerForm] = useState({ direction: 'IN', amount: '', description: '' })
  const [loans, setLoans] = useState<Loan[]>([])
  const [loansLoading, setLoansLoading] = useState(false)
  const [loansError, setLoansError] = useState('')
  const [loanFormOpen, setLoanFormOpen] = useState(false)
  const [loanForm, setLoanForm] = useState({ amount: '', tenorMonths: '6', purpose: '' })
  const [rejectLoanId, setRejectLoanId] = useState<string | null>(null)
  const [rejectionReason, setRejectionReason] = useState('')
  const [notice, setNoticeState] = useState<Notice | null>(null)
  const [loading, setLoading] = useState(false)
  const [members, setMembers] = useState<Member[]>([])
  const [membersLoading, setMembersLoading] = useState(false)
  const [membersError, setMembersError] = useState('')
  const [families, setFamilies] = useState<Family[]>([])
  const [memberFormOpen, setMemberFormOpen] = useState(false)
  const [memberMode, setMemberMode] = useState<'new-family' | 'new-member' | 'existing-member'>('new-member')
  const [memberSearch, setMemberSearch] = useState('')
  const [familySearch, setFamilySearch] = useState('')
  const [userResults, setUserResults] = useState<Array<{ id: string; name: string; email: string | null; phone: string }>>([])
  const [selectedUser, setSelectedUser] = useState<{ id: string; name: string; email: string | null; phone: string } | null>(null)
  const [memberForm, setMemberForm] = useState({ name: '', email: '', phone: '', password: '', role: 'MEMBER', familyId: '', familyName: '', familyCode: '', description: '' })
  const setNotice = (message: string | null) => setNoticeState(message ? { message, tone: 'success' } : null)
  const showNotice = (message: string, tone: Notice['tone'] = 'success') => setNoticeState({ message, tone })
  useEffect(() => { if (!notice) return; const timeout = window.setTimeout(() => setNoticeState(null), 5000); return () => window.clearTimeout(timeout) }, [notice])
  useEffect(() => { if (localStorage.getItem('dana_access_token')) api<{ data: User }>('/auth/me').then((payload) => setUser(payload.data)).catch(() => localStorage.removeItem('dana_access_token')) }, [])
  const loadMembers = async () => { setMembersLoading(true); setMembersError(''); try { setMembers((await api<{ data: Member[] }>('/management/members')).data) } catch (error) { setMembersError(error instanceof Error ? error.message : 'Data anggota tidak dapat dimuat') } finally { setMembersLoading(false) } }
  const loadLoans = async () => { setLoansLoading(true); setLoansError(''); try { setLoans((await api<{ data: Loan[] }>('/loans')).data) } catch (error) { setLoansError(error instanceof Error ? error.message : 'Data pinjaman tidak dapat dimuat') } finally { setLoansLoading(false) } }
  const loadLedger = async () => { setLedgerLoading(true); try { setLedger((await api<{ data: LedgerEntry[] }>('/ledger')).data) } catch { setLedger([]) } finally { setLedgerLoading(false) } }
  useEffect(() => { if (!user) return; api<{ data: Summary }>('/dashboard/summary').then((payload) => setSummary(payload.data)).catch(() => undefined); loadLoans(); loadMembers(); loadLedger(); if (user.systemRole === 'SUPER_ADMIN') api<{ data: Family[] }>('/management/families').then((payload) => { setFamilies(payload.data); setMemberForm((current) => ({ ...current, familyId: current.familyId || payload.data[0]?.id || '' })) }).catch(() => undefined) }, [user])
  const login = async (event: FormEvent) => { event.preventDefault(); setLoginLoading(true); setLoginError(''); try { const payload = await api<{ data: { accessToken: string; refreshToken: string; user: User } }>('/auth/login', { method: 'POST', body: JSON.stringify(loginForm) }); localStorage.setItem('dana_access_token', payload.data.accessToken); localStorage.setItem('dana_refresh_token', payload.data.refreshToken); setUser(payload.data.user) } catch (error) { setLoginError(error instanceof Error ? error.message : 'Login gagal') } finally { setLoginLoading(false) } }
  const logout = async () => { const refreshToken = localStorage.getItem('dana_refresh_token'); await api('/auth/logout', { method: 'POST', body: JSON.stringify({ refreshToken }) }).catch(() => undefined); localStorage.removeItem('dana_access_token'); localStorage.removeItem('dana_refresh_token'); setUser(null) }
  const submitRegistration = async (event: FormEvent) => { event.preventDefault(); setLoading(true); setNotice(null); try { if (user?.systemRole === 'SUPER_ADMIN') { const type = memberMode === 'new-family' ? 'NEW_FAMILY' : memberMode === 'existing-member' ? 'EXISTING_MEMBER' : 'NEW_MEMBER'; await api('/management/registrations', { method: 'POST', body: JSON.stringify({ type, role: memberForm.role, familyId: memberForm.familyId || undefined, existingUserId: selectedUser?.id, name: memberForm.name || undefined, email: memberForm.email || undefined, phone: memberForm.phone || undefined, password: memberForm.password || undefined, familyName: memberForm.familyName || undefined, familyCode: memberForm.familyCode || undefined, description: memberForm.description || undefined }) }) } else { await api('/management/members', { method: 'POST', body: JSON.stringify({ role: memberForm.role, name: memberForm.name, email: memberForm.email || undefined, phone: memberForm.phone, password: memberForm.password }) }) } setMemberFormOpen(false); setSelectedUser(null); setMemberSearch(''); setUserResults([]); setMemberForm({ name: '', email: '', phone: '', password: '', role: 'MEMBER', familyId: memberForm.familyId, familyName: '', familyCode: '', description: '' }); await loadMembers(); showNotice('Anggota berhasil ditambahkan ke ruang keluarga.') } catch (error) { showNotice(error instanceof Error ? error.message : 'Pendaftaran gagal', 'error') } finally { setLoading(false) } }
  const searchExistingUsers = async (value: string) => { setMemberSearch(value); setSelectedUser(null); if (value.trim().length < 2) return setUserResults([]); try { setUserResults((await api<{ data: Array<{ id: string; name: string; email: string | null; phone: string }> }>(`/management/users?search=${encodeURIComponent(value)}`)).data) } catch { setUserResults([]) } }
  const submitLoan = async (event: FormEvent) => { event.preventDefault(); setLoading(true); setNotice(null); try { await api('/loans', { method: 'POST', body: JSON.stringify({ amount: loanForm.amount, tenorMonths: loanForm.tenorMonths, purpose: loanForm.purpose }) }); setLoanFormOpen(false); setLoanForm({ amount: '', tenorMonths: '6', purpose: '' }); await loadLoans(); showNotice('Pengajuan pinjaman berhasil dibuat.') } catch (error) { showNotice(error instanceof Error ? error.message : 'Pengajuan gagal', 'error') } finally { setLoading(false) } }
  const submitLedger = async (event: FormEvent) => { event.preventDefault(); setLoading(true); try { await api('/ledger', { method: 'POST', body: JSON.stringify({ direction: ledgerForm.direction, amount: ledgerForm.amount, description: ledgerForm.description }) }); setLedgerFormOpen(false); setLedgerForm({ direction: 'IN', amount: '', description: '' }); await loadLedger(); showNotice('Catatan kas berhasil disimpan.') } catch (error) { showNotice(error instanceof Error ? error.message : 'Catatan kas gagal disimpan', 'error') } finally { setLoading(false) } }
  const updateLoan = async (id: string, action: 'approve' | 'disburse' | 'reject', reason?: string) => { setLoading(true); try { await api(`/loans/${id}/${action}`, { method: 'POST', body: action === 'reject' ? JSON.stringify({ reason }) : undefined }); setRejectLoanId(null); setRejectionReason(''); await loadLoans(); showNotice(`Pinjaman berhasil ${action === 'approve' ? 'disetujui' : action === 'disburse' ? 'dicairkan' : 'ditolak'}.`) } catch (error) { showNotice(error instanceof Error ? error.message : 'Aksi gagal', 'error') } finally { setLoading(false) } }
  const submitRejection = async (event: FormEvent) => { event.preventDefault(); if (!rejectLoanId) return; await updateLoan(rejectLoanId, 'reject', rejectionReason) }
  const unpaidInstallments = loans.reduce((total, loan) => total + loan.installments.filter((installment) => installment.status !== 'PAID').length, 0)
  const nav = [{ label: 'Ringkasan', icon: LayoutDashboard }, { label: 'Kas', icon: WalletCards }, { label: 'Pinjaman', icon: HandCoins }, { label: 'Cicilan', icon: CalendarClock, count: unpaidInstallments }, { label: 'Anggota', icon: Users }]
  const isSuperAdmin = user?.systemRole === 'SUPER_ADMIN'
  const isFamilyAdmin = user?.familyRole === 'ADMIN'
  const isTreasurer = user?.familyRole === 'TREASURER'
  const isMember = user?.systemRole === 'USER' && user?.familyRole === 'MEMBER'
  const canManageMembers = isSuperAdmin || isFamilyAdmin
  const canManageLoans = isSuperAdmin || isFamilyAdmin || isTreasurer
  const canRequestLoan = isMember
  const canManageLedger = canManageLoans
  const filteredFamilies = families.filter((family) => `${family.name} ${family.code}`.toLowerCase().includes(familySearch.trim().toLowerCase()))
  const installmentGroups = loans.reduce<Array<{ borrowerId: string; borrowerName: string; loans: Loan[]; installments: Array<{ loan: Loan; installment: Installment }> }>>((groups, loan) => { const existing = groups.find((group) => group.borrowerId === loan.borrower.id); const group = existing ?? { borrowerId: loan.borrower.id, borrowerName: loan.borrower.name, loans: [], installments: [] }; if (!existing) groups.push(group); group.loans.push(loan); loan.installments.forEach((installment) => group.installments.push({ loan, installment })); return groups }, [])
  const [expandedBorrowers, setExpandedBorrowers] = useState<string[]>([])
  const openMemberForm = () => { setMemberMode('new-member'); setFamilySearch(''); setMemberForm((current) => ({ ...current, password: '' })); setMemberFormOpen(true) }
  if (!user) return <div className="login-page"><form className="login-card" onSubmit={login}><div className="brand">Dana <i>Keluarga</i></div><p className="eyebrow">RUANG BERSAMA</p><h1>Selamat datang kembali.</h1><p className="login-copy">Masuk untuk melihat kas, pinjaman, dan aktivitas keluarga.</p>{loginError && <div className="notice login-error">{loginError}</div>}<label>Email<input required type="email" value={loginForm.email} onChange={(event) => setLoginForm({ ...loginForm, email: event.target.value })} /></label><label>Password<input required type="password" minLength={8} value={loginForm.password} onChange={(event) => setLoginForm({ ...loginForm, password: event.target.value })} /></label><button className="primary login-submit" disabled={loginLoading}>{loginLoading ? 'Memeriksa...' : 'Masuk'}</button></form></div>
  return <div className="app-shell">
    <aside className={menuOpen ? 'sidebar open' : 'sidebar'}>
      <div className="brand">Dana <i>Keluarga</i><button className="icon-button close-menu" onClick={() => setMenuOpen(false)} aria-label="Tutup menu"><X size={18} /></button></div>
      <div className="family-switcher"><span className="avatar coral">{user.systemRole === 'SUPER_ADMIN' ? 'SA' : (user.familyName?.slice(0, 1) ?? 'K')}</span><div><strong>{user.systemRole === 'SUPER_ADMIN' ? 'Administrasi global' : user.familyName ?? 'Ruang keluarga'}</strong><small>{user.systemRole === 'SUPER_ADMIN' ? 'Super Admin' : 'Ruang bersama'}</small></div>{user.systemRole !== 'SUPER_ADMIN' && <ChevronDown size={16} />}</div>
      <p className="eyebrow">MENU UTAMA</p>
      <nav aria-label="Navigasi utama">{nav.map(({ label, icon: Icon, count }) => <button className={active === label ? 'nav-item active' : 'nav-item'} key={label} onClick={() => { setActive(label); setMenuOpen(false) }}><Icon size={18} />{label}{count && <span className="nav-count">{count}</span>}</button>)}</nav>
      <p className="eyebrow secondary">LAINNYA</p><button className="nav-item"><Bell size={18} />Notifikasi</button><button className="nav-item"><Settings size={18} />Pengaturan</button>
      <div className="help"><CircleHelp size={19} /><div><strong>Butuh bantuan?</strong><small>Baca panduan singkat</small></div><ArrowUpRight size={15} /></div>
      <div className="account-area"><button className="profile" onClick={() => setAccountMenuOpen((open) => !open)} aria-expanded={accountMenuOpen}><span className="avatar indigo">{user.name.slice(0, 2).toUpperCase()}</span><div><strong>{user.name}</strong><small>{user.systemRole === 'SUPER_ADMIN' ? 'Super Admin' : user.familyRole === 'ADMIN' ? 'Admin Keluarga' : user.familyRole === 'TREASURER' ? 'Treasurer' : 'Anggota'}</small></div><ChevronDown size={15} /></button>{accountMenuOpen && <div className="account-menu"><div><strong>{user.name}</strong><small>{user.email || user.phone}</small></div><button onClick={logout}><ArrowUpRight size={15} />Keluar dari akun</button></div>}</div>
    </aside>
    <main className="content"><header><button className="icon-button mobile-menu" onClick={() => setMenuOpen(true)} aria-label="Buka menu"><Menu size={20} /></button><div className="crumb">Ruang bersama <span>/</span> <strong>{active}</strong></div><div className="header-actions"><button className="icon-button" aria-label="Notifikasi"><Bell size={19} /><b>2</b></button><span className="avatar indigo">{user.name.slice(0, 2).toUpperCase()}</span></div></header>
      <section className="intro"><div><p className="date">Senin, 31 Agustus 2026</p><h1>{active === 'Anggota' ? 'Kelola ruang bersama.' : active === 'Pinjaman' ? (canManageLoans ? 'Kelola pinjaman.' : 'Pinjaman saya.') : active === 'Cicilan' ? 'Jadwal cicilan.' : 'Selamat pagi, Andi.'}</h1><p>{active === 'Anggota' ? 'Pastikan setiap orang memiliki akses dan peran yang tepat di keluarga ini.' : active === 'Pinjaman' ? (canManageLoans ? 'Tinjau pengajuan dan kelola dana keluarga dengan tertib.' : 'Pantau pengajuan dan kewajiban pinjaman Anda.') : active === 'Cicilan' ? 'Lihat jadwal pembayaran berdasarkan pinjaman yang disetujui.' : 'Pelan-pelan, yang penting bersama. Ini kabar terbaru ruang dana keluarga.'}</p></div>{active === 'Anggota' && canManageMembers && <button className="primary" onClick={openMemberForm}><UserPlus size={17} />Tambah anggota</button>}</section>
      {notice && <div className={`toast toast-${notice.tone}`} role="status"><span>{notice.tone === 'success' ? 'Berhasil' : notice.tone === 'warning' ? 'Perhatian' : 'Tidak berhasil'}</span><p>{notice.message}</p><button className="toast-close" onClick={() => setNotice(null)} aria-label="Tutup notifikasi"><X size={16} /></button></div>}
      {active === 'Pinjaman' && <section className="panel loan-list"><div className="panel-heading"><div><p className="eyebrow">RUANG DANA</p><h2>{canManageLoans ? 'Kelola pinjaman' : 'Pinjaman saya'}</h2><p className="panel-description">{canManageLoans ? 'Tinjau pengajuan dan kelola pencairan dana keluarga.' : 'Pantau status pengajuan dan jadwal pengembalian dana Anda.'}</p></div>{canRequestLoan && <button className="primary" onClick={() => setLoanFormOpen(true)}><HandCoins size={15} />Ajukan pinjaman</button>}</div>{loansLoading ? <p className="empty">Memuat data pinjaman...</p> : loansError ? <div className="member-error"><p>{loansError}</p><button className="secondary-button" onClick={loadLoans}>Coba lagi</button></div> : loans.length === 0 ? <p className="empty">Belum ada data pinjaman.</p> : loans.map((loan) => <div className="loan-row" key={loan.id}><div><strong>{loan.borrower.name}</strong><small>{loan.purpose} · {loan.tenorMonths} bulan · Diajukan {date.format(new Date(loan.requestedAt))}</small>{loan.status === 'REJECTED' && loan.rejectionReason && <small className="rejection-reason">Alasan: {loan.rejectionReason}</small>}</div><b>{rupiah(loan.principalAmount)}</b><span className={`status ${loan.status.toLowerCase()}`}>{loanStatus[loan.status] ?? loan.status}</span>{canManageLoans && <div className="loan-actions">{loan.status === 'PENDING' && <><button onClick={() => updateLoan(loan.id, 'approve')}>Setujui</button><button onClick={() => { setRejectLoanId(loan.id); setRejectionReason('') }}>Tolak</button></>}{loan.status === 'APPROVED' && <button onClick={() => updateLoan(loan.id, 'disburse')}>Cairkan</button>}</div>}</div>)}</section>}
      {active === 'Cicilan' && <section className="panel loan-list installment-list"><div className="panel-heading"><div><p className="eyebrow">JADWAL PEMBAYARAN</p><h2>{canManageLoans ? 'Cicilan keluarga' : 'Cicilan saya'}</h2><p className="panel-description">Pilih nama untuk melihat detail pinjaman dan jadwal cicilannya.</p></div></div>{loansLoading ? <p className="empty">Memuat jadwal cicilan...</p> : loansError ? <div className="member-error"><p>{loansError}</p><button className="secondary-button" onClick={loadLoans}>Coba lagi</button></div> : installmentGroups.length === 0 ? <p className="empty">Belum ada jadwal cicilan.</p> : <div className="installment-groups">{installmentGroups.map((group) => { const expanded = expandedBorrowers.includes(group.borrowerId); const remaining = group.installments.reduce((total, item) => total + Number(item.installment.remainingAmount), 0); const unpaid = group.installments.filter((item) => item.installment.status !== 'PAID').length; return <article className={expanded ? 'installment-group expanded' : 'installment-group'} key={group.borrowerId}><button className="installment-group-header" onClick={() => setExpandedBorrowers((current) => expanded ? current.filter((id) => id !== group.borrowerId) : [...current, group.borrowerId])} aria-expanded={expanded}><span className="avatar coral">{group.borrowerName.slice(0, 1).toUpperCase()}</span><span className="installment-group-person"><strong>{group.borrowerName}</strong><small>{group.loans.length} pinjaman · {unpaid} cicilan belum lunas</small></span><b>{rupiah(remaining)}</b><ChevronDown size={18} /></button>{expanded && <div className="installment-group-detail">{group.loans.map((loan) => <div className="installment-loan" key={loan.id}><div className="installment-loan-heading"><div><strong>{loan.purpose}</strong><small>{rupiah(loan.principalAmount)} · {loan.tenorMonths} bulan · {loanStatus[loan.status] ?? loan.status}</small></div><span className={`status ${loan.status.toLowerCase()}`}>{loanStatus[loan.status] ?? loan.status}</span></div><div className="installment-detail-list">{loan.installments.map((installment) => <div className="installment-detail-row" key={installment.id}><span>Cicilan {installment.installmentNumber}</span><small>Jatuh tempo {date.format(new Date(installment.dueDate))}</small><b>{rupiah(installment.remainingAmount)}</b><span className={`status ${installment.status.toLowerCase()}`}>{installmentStatus[installment.status] ?? installment.status}</span></div>)}</div></div>)}</div>}</article> })}</div>}</section>}
      {active === 'Anggota' && <><section className="member-stats"><article><span className="stat-icon coral-light"><Users size={17} /></span><div><strong>{members.length}</strong><small>Total anggota</small></div></article><article><span className="stat-icon green-light"><ShieldCheck size={17} /></span><div><strong>{members.filter((member) => member.status === 'ACTIVE').length}</strong><small>Akses aktif</small></div></article><article><span className="stat-icon neutral-light"><HandCoins size={17} /></span><div><strong>{members.filter((member) => member.role === 'TREASURER').length}</strong><small>Pengelola dana</small></div></article></section><section className="panel member-list"><div className="panel-heading"><div><p className="eyebrow">RUANG KELUARGA</p><h2>Daftar anggota</h2><p className="panel-description">Periksa identitas dan akses setiap orang di ruang ini.</p></div></div>{membersLoading ? <p className="empty">Memuat data anggota...</p> : membersError ? <div className="member-error"><p>{membersError}</p><button className="secondary-button" onClick={loadMembers}>Coba lagi</button></div> : members.length === 0 ? <p className="empty">Belum ada anggota di ruang keluarga ini.</p> : <div className="member-table"><div className="member-table-head"><span>ANGGOTA</span><span>PERAN</span><span>STATUS</span><span>TERDAFTAR</span></div>{members.map((member) => <div className="member-row" key={member.id}><div className="member-identity"><span className="avatar coral">{member.user.name.slice(0, 1).toUpperCase()}</span><div><strong>{member.user.name}</strong><small>{member.user.email || member.user.phone}</small></div></div><span className="role-label">{member.role === 'ADMIN' ? 'Admin keluarga' : member.role === 'TREASURER' ? 'Pengelola dana' : 'Anggota'}</span><span className={`status ${member.status.toLowerCase()}`}>{member.status === 'ACTIVE' ? 'Aktif' : member.status}</span><small className="joined-date">{date.format(new Date(member.joinedAt))}</small></div>)}</div>}</section></>}
      {active === 'Ringkasan' && <section className="welcome"><div><p className="kicker">RUANG DANA</p><h2>Ringkasan kas keluarga.</h2><p>Saldo dan aktivitas di bawah ini berasal dari catatan kas keluarga yang tersimpan di database.</p><button className="text-button" onClick={() => setActive('Kas')}>Buka catatan kas <ArrowUpRight size={15} /></button></div></section>}
      {active === 'Kas' && <section className="panel ledger-list"><div className="panel-heading"><div><p className="eyebrow">CATATAN KAS</p><h2>Arus kas keluarga</h2><p className="panel-description">Catat pemasukan dan pengeluaran agar saldo pencairan selalu akurat.</p></div>{canManageLedger && <button className="primary" onClick={() => setLedgerFormOpen(true)}><WalletCards size={15} />Catat kas</button>}</div>{ledgerLoading ? <p className="empty">Memuat catatan kas...</p> : ledger.length === 0 ? <p className="empty">Belum ada catatan kas.</p> : <div className="ledger-rows">{ledger.map((entry) => <div className="ledger-row" key={entry.id}><span className={entry.direction === 'IN' ? 'ledger-icon incoming' : 'ledger-icon outgoing'}>{entry.direction === 'IN' ? <ArrowDownRight size={16} /> : <ArrowUpRight size={16} />}</span><div><strong>{entry.description}</strong><small>{date.format(new Date(entry.occurredAt))} · {entry.createdBy?.name ?? 'Pengelola'}</small></div><b className={entry.direction === 'IN' ? 'positive' : 'negative'}>{entry.direction === 'IN' ? '+' : '−'} {rupiah(entry.amount)}</b></div>)}</div>}</section>}
      {active === 'Ringkasan' && <><div className="section-heading"><div><p className="eyebrow">POSISI DANA</p><h2>Gambaran ruang bersama</h2></div></div><section className="metrics"><article className="metric primary-metric"><p>SALDO BERSAMA</p><small>Saldo tersedia</small><strong>{summary ? rupiah(summary.balance) : 'Belum tersedia'}</strong><span className="positive"><ArrowUpRight size={14} /> ruang dana aktif</span></article><article className="metric"><p>Sedang dipinjamkan</p><strong>{summary ? rupiah(summary.loans) : 'Belum tersedia'}</strong><small>berdasarkan data aktif</small></article><article className="metric"><p>Cicilan bulan ini</p><strong>{summary ? rupiah(summary.installments) : 'Belum tersedia'}</strong><small>dari jadwal pembayaran</small></article></section><section className="panel ledger-list"><div className="panel-heading"><div><p className="eyebrow">AKTIVITAS TERBARU</p><h2>Arus kas terbaru</h2></div><button className="text-button" onClick={() => setActive('Kas')}>Lihat semua <ArrowUpRight size={15} /></button></div>{ledger.length === 0 ? <p className="empty">Belum ada aktivitas kas.</p> : <div className="ledger-rows">{ledger.slice(0, 5).map((entry) => <div className="ledger-row" key={entry.id}><span className={entry.direction === 'IN' ? 'ledger-icon incoming' : 'ledger-icon outgoing'}>{entry.direction === 'IN' ? <ArrowDownRight size={16} /> : <ArrowUpRight size={16} />}</span><div><strong>{entry.description}</strong><small>{date.format(new Date(entry.occurredAt))}</small></div><b className={entry.direction === 'IN' ? 'positive' : 'negative'}>{entry.direction === 'IN' ? '+' : '−'} {rupiah(entry.amount)}</b></div>)}</div>}</section></>}
      {loanFormOpen && <div className="modal-backdrop"><form className="modal" onSubmit={submitLoan}><button type="button" className="icon-button modal-close" onClick={() => setLoanFormOpen(false)} aria-label="Tutup form"><X size={18} /></button><p className="eyebrow">PENGAJUAN PINJAMAN</p><h2>Ajukan pinjaman</h2><label>Jumlah pinjaman<div className="currency-input"><span>Rp</span><input required inputMode="numeric" value={rupiahInput(loanForm.amount)} onChange={(event) => setLoanForm({ ...loanForm, amount: event.target.value.replace(/\D/g, '') })} placeholder="0" /></div><small className="field-help">Nilai yang disimpan: angka rupiah tanpa format.</small></label><label>Tenor (bulan)<input required type="number" min="1" max="60" value={loanForm.tenorMonths} onChange={(event) => setLoanForm({ ...loanForm, tenorMonths: event.target.value })} /></label><label>Tujuan<textarea required minLength={3} value={loanForm.purpose} onChange={(event) => setLoanForm({ ...loanForm, purpose: event.target.value })} placeholder="Untuk apa dana digunakan?" /></label><button className="primary" disabled={loading}>{loading ? 'Mengirim...' : 'Kirim pengajuan'}</button></form></div>}
      {ledgerFormOpen && <div className="modal-backdrop"><form className="modal" onSubmit={submitLedger}><button type="button" className="icon-button modal-close" onClick={() => setLedgerFormOpen(false)} aria-label="Tutup form"><X size={18} /></button><p className="eyebrow">CATATAN KAS</p><h2>Catat arus kas</h2><label>Jenis catatan<select value={ledgerForm.direction} onChange={(event) => setLedgerForm({ ...ledgerForm, direction: event.target.value })}><option value="IN">Pemasukan</option><option value="OUT">Pengeluaran</option></select></label><label>Nominal<div className="currency-input"><span>Rp</span><input required inputMode="numeric" value={rupiahInput(ledgerForm.amount)} onChange={(event) => setLedgerForm({ ...ledgerForm, amount: event.target.value.replace(/\D/g, '') })} placeholder="0" /></div></label><label>Keterangan<textarea required minLength={3} value={ledgerForm.description} onChange={(event) => setLedgerForm({ ...ledgerForm, description: event.target.value })} placeholder="Contoh: Setoran bulanan keluarga" /></label><button className="primary" disabled={loading}>{loading ? 'Menyimpan...' : 'Simpan catatan'}</button></form></div>}
      {rejectLoanId && <div className="modal-backdrop"><form className="modal" onSubmit={submitRejection}><button type="button" className="icon-button modal-close" onClick={() => setRejectLoanId(null)} aria-label="Tutup form"><X size={18} /></button><p className="eyebrow">KEPUTUSAN PENGAJUAN</p><h2>Tolak pinjaman</h2><p className="modal-intro">Tuliskan alasan agar peminjam memahami keputusan ini.</p><label>Alasan penolakan<textarea required minLength={3} maxLength={240} value={rejectionReason} onChange={(event) => setRejectionReason(event.target.value)} placeholder="Contoh: Nominal melebihi batas kas keluarga." /></label><button className="primary" disabled={loading}>{loading ? 'Menyimpan...' : 'Konfirmasi penolakan'}</button></form></div>}
      {memberFormOpen && <div className="modal-backdrop"><form className="modal member-modal" onSubmit={submitRegistration}><button type="button" className="icon-button modal-close" onClick={() => setMemberFormOpen(false)} aria-label="Tutup form"><X size={18} /></button><p className="eyebrow">{user.systemRole === 'SUPER_ADMIN' ? 'ADMINISTRASI GLOBAL' : 'RUANG KELUARGA'}</p><h2>{user.systemRole === 'SUPER_ADMIN' ? 'Tambah akses keluarga' : 'Tambah anggota'}</h2><p className="modal-intro">{user.systemRole === 'SUPER_ADMIN' ? 'Pilih tindakan yang sesuai dengan status akun dan keluarga.' : 'Buat akun baru untuk orang yang belum terdaftar di ruang keluarga ini.'}</p>{user.systemRole === 'SUPER_ADMIN' && <div className="mode-switch three"><button type="button" className={memberMode === 'new-family' ? 'selected' : ''} onClick={() => { setMemberMode('new-family'); setSelectedUser(null) }}>Buat keluarga baru</button><button type="button" className={memberMode === 'new-member' ? 'selected' : ''} onClick={() => { setMemberMode('new-member'); setSelectedUser(null) }}>Buat akun anggota</button><button type="button" className={memberMode === 'existing-member' ? 'selected' : ''} onClick={() => setMemberMode('existing-member')}>Hubungkan akun lama</button></div>}{user.systemRole === 'SUPER_ADMIN' && memberMode === 'existing-member' ? <><label>Cari akun yang sudah terdaftar<input required minLength={2} value={memberSearch} onChange={(event) => searchExistingUsers(event.target.value)} placeholder="Nama, email, atau WhatsApp" /></label>{selectedUser && <div className="selected-user"><strong>{selectedUser.name}</strong><small>{selectedUser.email || selectedUser.phone}</small></div>}{userResults.map((candidate) => <button type="button" className="user-result" key={candidate.id} onClick={() => { setSelectedUser(candidate); setUserResults([]) }}><strong>{candidate.name}</strong><small>{candidate.email || candidate.phone}</small></button>)}</> : <><label>Nama lengkap<input required minLength={2} value={memberForm.name} onChange={(event) => setMemberForm({ ...memberForm, name: event.target.value })} placeholder="Contoh: Rani Kusuma" /></label><label>Email <span className="optional">opsional</span><input type="email" value={memberForm.email} onChange={(event) => setMemberForm({ ...memberForm, email: event.target.value })} placeholder="nama@email.com" /></label><label>Nomor WhatsApp<input required value={memberForm.phone} onChange={(event) => setMemberForm({ ...memberForm, phone: event.target.value })} placeholder="081234567890" /></label><label>Kata sandi sementara<div className="password-field"><input required minLength={8} type="text" autoComplete="off" value={memberForm.password} onChange={(event) => setMemberForm({ ...memberForm, password: event.target.value })} placeholder="Buat minimal 8 karakter" /><button type="button" className="generate-button" onClick={() => setMemberForm({ ...memberForm, password: temporaryPassword() })}>Buat acak</button></div><small className="field-help">Gunakan hanya sebagai kredensial awal dan sampaikan melalui jalur aman.</small></label>{memberMode === 'new-family' && <><label>Nama Admin Keluarga<input required value={memberForm.name} onChange={(event) => setMemberForm({ ...memberForm, name: event.target.value })} placeholder="Contoh: Dwi Shinta" /></label><label>Nama keluarga baru<input required value={memberForm.familyName} onChange={(event) => setMemberForm({ ...memberForm, familyName: event.target.value })} /></label><label>Kode keluarga<input required value={memberForm.familyCode} onChange={(event) => setMemberForm({ ...memberForm, familyCode: event.target.value.toUpperCase() })} placeholder="CONTOH-KELUARGA" /></label><label>Deskripsi keluarga<textarea value={memberForm.description} onChange={(event) => setMemberForm({ ...memberForm, description: event.target.value })} /></label></>}</>} {memberMode !== 'new-family' && <label>Peran dalam keluarga<select value={memberForm.role} onChange={(event) => setMemberForm({ ...memberForm, role: event.target.value })}><option value="MEMBER">Anggota</option><option value="TREASURER">Pengelola dana</option><option value="ADMIN">Admin keluarga</option></select></label>}{memberMode !== 'new-family' && user.systemRole === 'SUPER_ADMIN' && <label>Keluarga tujuan<div className="family-picker"><input value={familySearch} onChange={(event) => setFamilySearch(event.target.value)} placeholder="Cari nama atau kode keluarga" />{familySearch && <div className="family-results">{filteredFamilies.length === 0 ? <small>Tidak ada keluarga ditemukan.</small> : filteredFamilies.map((family) => <button type="button" key={family.id} className={memberForm.familyId === family.id ? 'selected' : ''} onClick={() => { setMemberForm({ ...memberForm, familyId: family.id }); setFamilySearch(family.name) }}><strong>{family.name}</strong><small>{family.code}</small></button>)}</div>}</div>{memberForm.familyId && <small className="field-help">Keluarga dipilih: {families.find((family) => family.id === memberForm.familyId)?.name}</small>}</label>}{memberMode !== 'new-family' && <button className="primary" disabled={loading}>{loading ? 'Menyimpan...' : 'Simpan akses'}</button>}</form></div>}
      <footer>© 2026 Dana Keluarga <span>◈ Data ruang ini hanya untuk keluarga</span></footer>
    </main>
  </div>
}
export default App
